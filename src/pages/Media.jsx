import React from 'react';

const Media = () => {
    return (
        <div className="p-8">
            <h1 className="text-3xl font-bold mb-4">Media Library</h1>
            <p className="text-[var(--text-secondary)]">Manage photos and videos (Admin Only).</p>
        </div>
    );
};

export default Media;
