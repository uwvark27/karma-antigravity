import React from 'react';

const Events = () => {
    return (
        <div className="p-8">
            <h1 className="text-3xl font-bold mb-4">Events Calendar</h1>
            <p className="text-[var(--text-secondary)]">Manage upcoming family events (Admin Only).</p>
        </div>
    );
};

export default Events;
