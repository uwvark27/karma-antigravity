import React from 'react';

const Beer = () => {
    return (
        <div className="p-8">
            <h1 className="text-3xl font-bold mb-4">Beer Management</h1>
            <p className="text-[var(--text-secondary)]">Inventory and brewing logs (Admin Only).</p>
        </div>
    );
};

export default Beer;
